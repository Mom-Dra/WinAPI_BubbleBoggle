#include "AI.h"

namespace MomDra
{
	/*AI::AI()
	{
		
	}

	void AI::AddState(std::unique_ptr<AIState> state)
	{
		state->SetAI(this);
		stateMap.emplace(state->GetType(), std::move(state));
	}

	void AI::Update()
	{

	}*/
}