#pragma once
#include "Tag.h"

namespace MomDra
{
	/*class AI;

	class AIState
	{
	private:
		AI* ai;
		MonsterState monsterState;

	public:
		explicit AIState(MonsterState monsterState) noexcept;
		virtual ~AIState() = default;

		virtual void Enter() = 0;
		virtual void Exit() = 0;
		virtual void Update() = 0;

		inline AI* GetAI() const noexcept { return ai; }
		inline const MonsterState& GetType() const noexcept { return monsterState; }
		inline void SetAI(AI* ai) { this->ai = ai; }
	};*/
}
