#pragma once

namespace MomDra
{
	enum class Layer
	{
		DEFAULT,
		PLAYER,
		MISSILE,
		MONSTER,
		LAST
	};

	enum class BrushType
	{
		HOLLOW,
		LAST
	};

	enum class PenType
	{
		RED,
		GREEN,
		BLUE,
		LAST
	};
}
